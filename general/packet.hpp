#ifndef MC_packet
#define MC_packet

/*struct packet
{
    int   itsID;
    int   itsLenght;
    void* itsData;
    void* nxtData; // may be not used
};
typedef packet pckt;

struct buffered_packet
{
    int   size;
    void* data;
};
typedef buffered_packet buf_pckt;

buffered_packet emit_packet(char *& buffer, packet & pckt);*/

#endif // MC_packet
